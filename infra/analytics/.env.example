# Centralized defaults for both docker-compose and Terraform deployments.
# Copy to `.env` and override per-environment secrets via direnv or 1Password.
UMAMI_DB_NAME=umami
UMAMI_DB_USER=umami
UMAMI_DB_PASSWORD=umami-password
UMAMI_APP_SECRET=replace-me-with-long-random-value
UMAMI_PORT=3000
ANALYTICS_HOSTNAME=analytics.local.apotheon.ai
ANALYTICS_ADMIN_EMAIL=analytics-admin@apotheon.ai
